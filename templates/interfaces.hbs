import { ModuleMetadata, Type } from '@nestjs/common';

export interface {{this.pascal}}Options {
  example: string;
}

export interface {{this.pascal}}OptionsFactory {
  createOptions(): Promise<{{this.pascal}}Options> | {{this.pascal}}Options;
}

export interface {{this.pascal}}AsyncOptions extends Pick<ModuleMetadata, 'imports'> {
  useExisting?: Type<{{this.pascal}}OptionsFactory>;
  useClass?: Type<{{this.pascal}}OptionsFactory>;
  useFactory?: (
    ...args: any[]
  ) => Promise<{{this.pascal}}Options> | {{this.pascal}}Options;
  inject?: any[];
}
